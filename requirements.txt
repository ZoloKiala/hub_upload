# Intentionally empty.
#
# The app in pwa/ is static files, and serve_pwa.py uses only the standard
# library. This file stays so Nixpacks recognises a Python project and provides
# an interpreter for the start command in railway.json; adding a dependency here
# would mean the app had stopped being static.
